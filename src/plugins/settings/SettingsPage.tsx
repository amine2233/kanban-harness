import { Card, PageHeader } from '@/design-system'

export function SettingsPage() {
  return (
    <>
      <PageHeader title="Settings" />
      <Card title="Placeholder">A stateless plugin: nav entry + route, no Redux slice.</Card>
    </>
  )
}
