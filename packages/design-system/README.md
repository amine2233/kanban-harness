# @mvp/design-system

Reusable components over Heroku's purple3 CSS. The only code that knows `hk-*` classes.
Pure UI: no Redux, no API, no router. Import from the package root only.

```ts
import { Button, Card, Markdown, Modal } from '@mvp/design-system'
```
